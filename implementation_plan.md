# خطة التنفيذ (Implementation Plan)

**اسم المشروع:** Tony Visuals Portfolio
**بيئة العمل:** Next.js (App Router), Tailwind CSS, MongoDB, Cloudinary

تقسم هذه الخطة عملية تطوير المشروع إلى 6 مراحل أساسية لضمان بناء النظام بشكل منظم وفعال.

## المرحلة الأولى: التأسيس وإعداد البيئة (Project Setup & Configuration)
**الهدف:** تجهيز البنية التحتية للمشروع وتثبيت المكتبات المطلوبة.

1. **إنشاء مشروع Next.js:** 
   - استخدام `create-next-app` مع تفعيل (App Router) و (TypeScript) و (Tailwind CSS).
2. **تثبيت الحزم (Dependencies):**
   - قواعد البيانات: `mongoose`
   - رفع الصور: `cloudinary`
   - واجهة المستخدم: مكتبة Masonry (مثل `react-photo-album` أو استخدام CSS Columns).
3. **تهيئة Tailwind CSS:**
   - نقل الألوان والخطوط من (UI/UX Design Brief) إلى `tailwind.config.ts`.
   - إعداد الخطوط العالمية (Google Fonts) في `app/layout.tsx`.
4. **تجهيز متغيرات البيئة (`.env.local`):**
   - إضافة روابط MongoDB، مفاتيح Cloudinary، وكلمة المرور الخاصة بلوحة التحكم.

## المرحلة الثانية: قواعد البيانات والواجهة الخلفية (Backend & Database)
**الهدف:** ربط قاعدة البيانات وتأسيس النماذج (Models).

1. **إعداد الاتصال بقاعدة البيانات:**
   - إنشاء ملف `lib/mongodb.ts` لإدارة الاتصال بـ MongoDB.
2. **إنشاء النماذج (Models):**
   - تطبيق هيكل `Album` و `Image` المذكور في وثيقة (Backend Schema).
3. **تجهيز مسارات واجهة برمجة التطبيقات (API Routes):**
   - `GET /api/albums`: لجلب الألبومات.
   - إعداد دوال مساعدة للاتصال بـ Cloudinary لاحقاً.

## المرحلة الثالثة: الواجهة الأمامية العامة (Public UI Development)
**الهدف:** بناء الصفحات التي يراها الزوار العاديون (بدون ديناميكية كاملة في البداية).

1. **المكونات الأساسية (Layout & Shared Components):**
   - بناء شريط التنقل (Header) والتذييل (Footer).
2. **الصفحة الرئيسية (`app/page.tsx`):**
   - بناء قسم الـ Hero بتأثيرات الكاميرا (HUD).
   - بناء قسم About.
   - بناء شبكة الألبومات (Albums Grid) بشكل ثابت (Hardcoded) مبدئياً للتأكد من التصميم.

## المرحلة الرابعة: الربط الديناميكي وعرض الصور (Dynamic Routing & Masonry)
**الهدف:** جعل الموقع حياً وجلب البيانات الحقيقية من قاعدة البيانات.

1. **ربط الصفحة الرئيسية:**
   - استدعاء بيانات الألبومات من قاعدة البيانات وعرضها في شبكة الألبومات.
2. **إنشاء صفحة تفاصيل الحدث (`app/albums/[id]/page.tsx`):**
   - جلب الصور الخاصة بالألبوم المحدد.
   - بناء شبكة الصور المتداخلة (Masonry Grid) للحفاظ على الأبعاد العرضية والطولية.
   - تطبيق `Next/Image` مع خاصية `blurDataURL` (Lazy Loading) لتحسين الأداء.

## المرحلة الخامسة: لوحة التحكم والحماية (Admin Dashboard & Security)
**الهدف:** بناء لوحة التحكم المخفية لتوني.

1. **تأمين المسار المخفي:**
   - إعداد `middleware.ts` لحماية المسار `/t-secret-dashboard` ومسارات الـ API المحمية.
   - إنشاء صفحة تسجيل الدخول الثابتة (Hardcoded Password Auth).
2. **واجهة لوحة التحكم:**
   - بناء صفحة استعراض الألبومات وإدارتها.
3. **وظائف الرفع (Upload & Create):**
   - بناء واجهة إضافة ألبوم جديد (رفع صورة الغلاف عبر Cloudinary وحفظ البيانات).
   - بناء واجهة الرفع المتعدد (Bulk Upload) لصور الألبوم.
4. **وظائف الحذف (Deletion):**
   - برمجة وظيفة حذف الصور الفردية.
   - برمجة وظيفة حذف الألبوم بالكامل (Cascade Delete) من MongoDB و Cloudinary.

## المرحلة السادسة: الاختبار والرفع (Testing & Deployment)
**الهدف:** إطلاق الموقع على الإنترنت.

1. **المراجعة والأداء (Performance Audit):**
   - التأكد من سرعة تحميل الصور على الموبايل والديسكتوب.
   - مراجعة الـ Dark Mode والتجاوب (Responsive Design).
2. **الرفع على Vercel (Deployment):**
   - ربط مستودع Github بـ Vercel.
   - إضافة متغيرات البيئة (Environment Variables) في إعدادات Vercel.
   - النشر النهائي للموقع.