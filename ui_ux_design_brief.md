# وثيقة تصميم واجهة وتجربة المستخدم (UI/UX Design Brief)

**اسم المشروع:** Tony Visuals Portfolio
**التوجه الفني:** Atmospheric, Cinematic, Raw & Documentary (سينمائي، خام، توثيقي)

## 1. المفهوم البصري (Visual Concept)
يعتمد التصميم على محاكاة "عدسة الكاميرا" (Viewfinder) والبيئة السينمائية الخام لحفلات الأندرجراوند والمسارح. يتم تحقيق ذلك من خلال واجهات داكنة جداً (Dark Mode)، لمسات من النيون المضيء، وعناصر واجهة مستوحاة من شاشات الكاميرات الاحترافية (HUD / Metadata tags).

## 2. لوحة الألوان (Color Palette)
تم استخراج الألوان بدقة من إعدادات Tailwind:

*   **الخلفية الأساسية (Background / Surface):** `#081F26` (كحلي/أزرق مخضر عميق جداً - يعكس عتمة المسرح).
*   **خلفية الحاويات والأقسام (Surface Container High / Variant):** `#236871` (تيل/أزرق مخضر باهت - لتمييز الأقسام والحدود).
*   **اللون المميز (Accent / Primary Container):** `#7EFC9F` (أخضر نعناعي فاقع / Mint Green - يمثل إضاءة الليزر والنيون، يُستخدم في الأزرار، العناوين، والتأثيرات المضيئة).
*   **النصوص الفاتحة (On Surface / Inverse Surface):** `#FAFAFA` (أبيض ناصع/رمادي فاتح للقراءة المريحة).
*   **النصوص الداكنة (Inverse On Surface):** `#0B0B0B` (أسود - يُستخدم فوق الأزرار ذات اللون النعناعي).

## 3. التايپوجرافي (Typography)
يعتمد النظام على 3 عائلات من خطوط Google Fonts لتشكيل الهوية البصرية:

1.  **Syne (عناوين العرض - Display & Headlines):** 
    *   **الاستخدام:** العناوين الرئيسية الضخمة (Hero)، وأسماء الأقسام. 
    *   **الطابع:** عصري، عريض، وجريء جداً.
2.  **Space Grotesk (الملصقات والبيانات التقنية - Labels & Metadata):** 
    *   **الاستخدام:** الأزرار، الترويسة، تفاصيل الكاميرا (ISO, f-stop)، والتواريخ.
    *   **الطابع:** هندسي، تقني، ومناسب للأحرف الكبيرة (Uppercase) مع تباعد أحرف (Tracking/Letter-spacing) واسع.
3.  **Manrope (النصوص الأساسية - Body):** 
    *   **الاستخدام:** الفقرات، ونبذة About، ووصف الألبومات.
    *   **الطابع:** مقروء جداً، نظيف، وبسيط.

## 4. المكونات البصرية والتأثيرات (UI Components & Effects)

### أ. معالجة الصور (Image Treatments)
*   **التشبع والتباين (Desaturation):** جميع صور الألبومات يتم تطبيق فلتر عليها `grayscale(25%) contrast(105%)` لإعطائها طابعاً درامياً.
*   **التفاعل (Hover Zoom):** عند تمرير الماوس، تعود الألوان لطبيعتها `grayscale(0%)` وتكبر الصورة بنسبة طفيفة `scale(1.03)` بحركة ناعمة.

### ب. عناصر واجهة الكاميرا (HUD & Viewfinder UI)
*   **زوايا التركيز (HUD Brackets):** استخدام `::before` و `::after` لإنشاء زوايا خضراء (`#7EFC9F`) حول الصور الرئيسية لمحاكاة إطار التركيز في الكاميرا.
*   **مؤشر التسجيل (REC Indicator):** نقطة خضراء تومض `animate-pulse` بجوار نصوص تقنية مثل `REC // [FRAME 24A]`.
*   **البيانات الوصفية (Metadata Overlays):** مربعات شفافة `backdrop-blur-md` تطفو فوق الصور لعرض بيانات العدسة أو عدد الصور في الألبوم (مثال: `35MM SUMMILUX`).

### ج. الأزرار والتوهج (Buttons & Glows)
*   **الأزرار الرئيسية:** خلفية صلبة بلون Accent (`#7EFC9F`) مع نص أسود (`#0B0B0B`).
*   **تأثير التوهج (Neon Glow):** إضافة ظل مضيء للأزرار والعلامة التجارية: `shadow-[0_0_20px_rgba(126,252,159,0.3)]`.

## 5. تجربة المستخدم وهيكل الصفحة (UX Layout)
*   **شريط التنقل (Header):** ثابت (Sticky) في الأعلى مع خلفية زجاجية (Backdrop Blur) لضمان عدم تشتيت المستخدم أثناء التمرير.
*   **البورتفوليو (Albums Grid):** يعتمد نظام الشبكة (CSS Grid) مع بطاقات (Cards) منظمة. كل بطاقة تحتوي على عنوان، وصف قصير، موقع الحدث، وزر تفاعلي "VIEW SERIES".
*   **القسم التعريفي (About):** استخدام تخطيط غير متماثل (Asymmetrical Layout) لكسر الملل، مع صورة دائرية للمصور محاطة بهالة خضراء لافتة.
*   **التذييل (Footer):** بسيط، يركز على الروابط الاجتماعية (Instagram, Behance) بأسلوب نصي بحت بدون أيقونات مزعجة.

## 6. الأيقونات (Icons)
*   استخدام مكتبة `Material Symbols Outlined` للحصول على أيقونات دقيقة ورفيعة (مثل أيقونة الموقع `location_on`) تتماشى مع الطابع الهندسي للموقع.