# مسار عمل التطبيق (Application Flow)

**اسم المشروع:** Tony Visuals Portfolio

توضح هذه الوثيقة رحلة المستخدمين داخل التطبيق، سواء كانوا زواراً عاديين أو مدير الموقع (توني).

## 1. مسار الزائر العادي (Public User Journey)

الزائر العادي يدخل إلى الموقع لاستكشاف الأعمال ولا يتفاعل مع أي عمليات تسجيل دخول.

### أ. الصفحة الرئيسية (Home Page `/`)

1. **الوصول:** يفتح الزائر رابط الموقع.

2. **Hero Section:** يشاهد الزائر واجهة سينمائية جذابة تحتوي على اسم البراند (Tony Visuals) وزر للحجز/التواصل.

3. **About Section:** يمرر الزائر للأسفل (Scroll) ليقرأ نبذة عن المصور (توني) ويرى صورته الشخصية.

4. **Albums Grid (البورتفوليو):**

   * يرى الزائر شبكة من الألبومات (مقسمة كحفلات أو أفراح).

   * يعرض كل ألبوم صورة الغلاف (Cover Photo) وعنوان الحدث.

   * عند تمرير الماوس (Hover) على الألبوم، يظهر تأثير حركي بسيط لتشجيع الزائر على النقر.

### ب. صفحة تفاصيل الحدث (Event Details Page `/albums/[id]`)

1. **النقر:** يضغط الزائر على أحد الألبومات في الصفحة الرئيسية.

2. **التحميل:** ينتقل الزائر لصفحة جديدة مخصصة لهذا الحدث. تظهر الصور تدريجياً باستخدام تأثير (Blur) بفضل تقنية Lazy Loading.

3. **العرض:** يستعرض الزائر صور الحفلة المعروضة في شبكة متداخلة (Masonry Grid) تحافظ على أبعاد الصور الطولية والعرضية.

4. **التفاعل:** عند تمرير الماوس على أي صورة، يحدث تأثير (Zoom-in) خفيف.

## 2. مسار مدير الموقع (Admin Journey - Hidden Dashboard)

هذا المسار مخصص لتوني فقط لإدارة محتوى موقعه بسهولة وبشكل سري.

### أ. تسجيل الدخول (Authentication)

1. **الوصول المخفي:** يكتب توني المسار المخفي في المتصفح `t-dashboard/`.

2. **شاشة الحماية:** تظهر شاشة بسيطة تطلب إدخال "كلمة المرور".

3. **التحقق:**

   * إذا أدخل كلمة مرور خاطئة، تظهر رسالة خطأ.

   * إذا أدخل كلمة المرور الصحيحة، يقوم النظام بإنشاء جلسة (Cookie) وتوجيهه إلى لوحة التحكم.

### ب. إدارة الألبومات (Albums Management)

1. **لوحة التحكم الرئيسية:** بعد الدخول، يرى توني قائمة بالألبومات الحالية مع أزرار (إضافة ألبوم جديد، حذف ألبوم، تعديل ألبوم).

2. **إنشاء ألبوم:**

   * يضغط على "إضافة ألبوم جديد".

   * يُدخل "اسم الحدث" (مثال: حفلة مسرح ساقية الصاوي).

   * يختار "صورة الغلاف" ويضغط رفع.

   * يظهر الألبوم الجديد في القائمة.

3. **حذف ألبوم:** عند ضغط "حذف"، تظهر رسالة تأكيد، ثم يتم حذف الألبوم بجميع صوره من قاعدة البيانات ومن Cloudinary.

### ج. إدارة صور الألبوم (Photos Management)

1. **الدخول للألبوم:** يضغط توني على اسم ألبوم معين لفتحه من داخل لوحة التحكم.

2. **رفع الصور (Bulk Upload):**

   * يضغط على منطقة الرفع أو يقوم بسحب وإفلات (Drag & Drop) مجموعة من الصور.

   * يظهر شريط تقدم (Progress Bar) يوضح حالة رفع الصور.

3. **حذف صور فردية:** يمكنه رؤية جميع صور الألبوم المرفوعة، وبجانب كل صورة زر "حذف" لإزالتها إذا لزم الأمر.

## 3. مسار تدفق البيانات خلف الكواليس (System & Data Flow)

عندما يقوم توني برفع صورة جديدة داخل ألبوم:

1. يرسل المتصفح الصورة إلى مسار الـ API الداخلي في Next.js `api/upload/`.

2. يقوم الـ API في Next.js بإرسال الصورة إلى **Cloudinary**.

3. يعيد Cloudinary رابط الصورة (URL) ومعرفها (Public ID) إلى Next.js.

4. يقوم Next.js بحفظ الرابط والمعرف واسم الألبوم المرتبط بها في قاعدة بيانات **MongoDB**.

5. تظهر الصورة للمستخدمين في الموقع بناءً على الرابط المحفوظ في قاعدة البيانات.